for lib in QDUtilLib AD_dnSVM QuantumModelLib ConstPhys FOR_EVRT Tnum-Tana nDindex EVRT_dnSVM
do
  echo "-----" $lib
  [ ! -d $lib ] && continue
  echo "-----" $lib "exist"
  cd $lib
  make cleanall
  cd ..
done

